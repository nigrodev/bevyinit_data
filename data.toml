# TOML is only used for this file. The other files, such as templates, are in Rusty Object Notation (RON) format.
[bevyinit]
version = "0.0.1" # The data version. The local files will be reloaded if this changes.
bin_version = "0.2.0" # The binary version related to this. The user will be warned to update if this != the current installed binary version.
template_path = "templates" # ./templates/..

[data]
templates = [
  "window_settings",
  "bevy_xpbd_3d",
  "bevy_rapier3d",
  "minimal_13"
]
crates = []
repos = []